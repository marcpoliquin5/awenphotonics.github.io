Place your Lottie JSON animation(s) here.

Recommended filename: `abstract-apps.json` (was: "Abstract animation for apps.json").

Notes:
- This project expects the hero animation at `/assets/lottie/abstract-apps.json`.
- If your original file is named differently, rename/move it to the path above.
- If `prefers-reduced-motion` is enabled on the user agent, the site will show a subtle static gradient fallback instead of playing the animation.
